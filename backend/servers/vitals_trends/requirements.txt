# vitals_trends server — pin mcp identically across all servers + agent (PRD §8)
mcp>=1.27,<2
fastapi==0.136.*
uvicorn[standard]
pyjwt
# DB driver (used once the stub is replaced by the real connector on Jun 29)
psycopg[binary]
